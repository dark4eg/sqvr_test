const addLimit = l => (l ? `limit: [${l}]` : '');const addOffset = o => (o ? `, offset: [${o}]` : '');const addFilters = f => (f.length > 0 ? `, historyType: [${f}]` : '');const addUserId = u => (u ? `, userId: [${u}]` : '');export const GetHistoryRegistry = (limit, offset, filters, userId) => `query {        users {          history(${addLimit(limit)}${addOffset(offset)}${addFilters(filters)}${addUserId(  userId)}) {            date            historyType            systemText            documents {              id              mimetype            }            body            user {              id              firstName              lastName              avatarUrl            }            updater {              id              firstName              lastName              avatarUrl            }          }        }      }`;